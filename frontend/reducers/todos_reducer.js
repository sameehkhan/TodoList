import {RECEIVE_TODOS, RECEIVE_TODO} from '../actons/todo_actions';

const todosReducer = (state = {}, action) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default todosReducer;
